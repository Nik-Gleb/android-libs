# Java Gradle CircleCI 2.0 configuration file
#
# Check https://circleci.com/docs/2.0/language-java/ for more details#
#
version: 2
general:
  branches:
    only:
      - dev
machine:
  environment:
    REPO_OS_OVERRIDE: linux
jobs:
  build:
    docker:
      # specify the version you desire here
      - image: circleci/android:api-$PLATFORM_API-alpha
      #working_directory: ~/repo
    environment:
      JVM_OPTS: -Xmx3200m
      TERM: dumb
    steps:
      - checkout
      - run:
          name: prepare
          command: export RAW_COMMIT_MSG="$(git log --format=oneline -n 1 $CIRCLE_SHA1)" && export GIT_COMMIT_DESC="$(echo $RAW_COMMIT_MSG | grep -oP " [0-9].[0-9].[0-9].([0-9]|[0-9][0-9]|[0-9][0-9][0-9]|[0-9][0-9][0-9][0-9])")" && echo $GIT_COMMIT_DESC
#      - run:
#          name: prepare
#          command: ./update.sh
#      - restore_cache:
#          keys:
#            - v1-dependencies-{{ checksum "build.gradle" }}
#      - run:
#          name: deploy
#          command: ./gradlew assembleDebug
#      - run:
#          name: checkin
#          command: ./mark.sh
#      - save_cache:
#          paths:
#            - ~/.m2
#          key: v1-dependencies-{{ checksum "build.gradle" }}
